{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "oci-bassh version",
  "type": "object",
  "required": ["ok", "version", "commit", "date"],
  "properties": {
    "ok": {"type": "boolean"},
    "version": {"type": "string"},
    "commit": {"type": "string"},
    "date": {"type": "string"}
  }
}
